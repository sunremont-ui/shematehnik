node_modules
dist
*.local
.vite
*.tsbuildinfo

# WASM build artifacts (built locally / in CI via `npm run build:wasm`)
public/wasm/
wasm/build/

# test output
playwright-report/
test-results/
.last-run.json
